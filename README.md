# blackjack
eenentwintigen

//git clone:
git clone https://github.com/quinv/blackjack.git

//git updaten
git add *

git commit -m "message"

git push origin

//git status checken
git checkout 

git status (moet groen zien na add)